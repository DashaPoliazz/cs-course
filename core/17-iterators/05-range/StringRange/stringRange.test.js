const { it, describe } = require("node:test");
const assert = require("node:assert");
const StringRange = require("./StringRange.js");

describe("range of strings", () => {
  it("should ", () => {
    const iter = StringRange.new("a", "z");
  });
});
